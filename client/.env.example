# Client Configuration
# If deploying frontend and backend separately, set this to the deployed backend URL.
# If deploying as a single app (Render serving both), leave this blank or it will default to /api.
VITE_API_URL=http://localhost:5000/api
